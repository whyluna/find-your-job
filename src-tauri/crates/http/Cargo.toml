[package]
name = "fyj-http"
version = "0.1.0"
description = "FindYourJob 本地 HTTP API：仅监听 127.0.0.1，供浏览器扩展剪藏（P1）"
edition = "2021"

[dependencies]
fyj-core = { path = "../core" }
axum = "0.8"
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
tempfile = "3"
tower = "0.5"
http-body-util = "0.1"
